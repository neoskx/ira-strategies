# IRA Strategies — Claude Code

Read `AGENTS.md` for the full project conventions, architecture, and how-to guides.
Everything below is Claude Code-specific and supplements AGENTS.md.

## Claude Code Notes

- This project is vibe-coded — all changes go through AI agents, never manual edits.
- When running locally, use `python agents/orchestration/orchestrate.py run --dry-run`
  for a fast workflow check.
- `docs/index.html` is auto-generated; never suggest editing it directly.
- No personal data in code — this repo is public-facing.
