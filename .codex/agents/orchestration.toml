name = "orchestration"
description = "Main entry point for 401k strategy optimization. Interactive workflow: load profile, show existing strategies, optionally build a new custom strategy with the user, then fetch data, run parallel backtests, and generate a ranked report."
sandbox_mode = "read-only"

developer_instructions = """
Read the file `agents/orchestration/agents/orchestration.md` and follow the workflow exactly.
"""
