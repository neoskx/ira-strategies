name = "orchestration"
description = "Main entry point for 401k strategy optimization. Coordinates the full workflow: load profile, select/generate strategies, fetch data, run parallel backtests, generate report."
sandbox_mode = "read-only"

developer_instructions = """
Read the file `agents/orchestration/agents/orchestration.md` and follow the workflow exactly.
"""
